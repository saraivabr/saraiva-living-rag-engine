# SexyFlow — ciclo automático 2026-07-31 11:40Z

## Escopo

- Janela observada: últimas 3 horas.
- Conta Zernio: `6a1205a62b2567671a24e855`.
- Mídia: `18299164084305199`.
- Destino preservado: `https://comunidade.saraiva.ai/estruturas/prospeccao-automatizada`.

## Evidência

- 53 webhooks Zernio recebidos e 47 comandos concluídos.
- Zero falhas de private reply, zero erros Lambda e filas principal/DLQ vazias.
- Sete sessões da campanha foram atualizadas na janela.
- Duas jornadas chegaram a `offering_community`, com exemplo aberto e CTA
  enviado.
- Uma pergunta fora do roteiro foi respondida pelo Bedrock sem alterar
  `awaiting_goal`.
- Duas tentativas reais de áudio, já usando `upload-direct`, ainda receberam
  HTTP 400 no envio da URL temporária para a conversa. O fallback textual e o
  card foram entregues nas duas jornadas.
- A leitura do inbox confirmou que nenhum anexo de áudio foi criado.
- Nenhuma duplicação, perda de estado, fato inventado ou alteração indevida de
  destino foi observada.

## Hipótese

O upload e a hospedagem estão saudáveis: um novo MP3 foi aceito pelo Zernio e
servido por `media.zernio.com` com HTTP 200, `audio/mpeg`, extensão `.mp3` e
50.199 bytes. A rejeição ocorre na etapa posterior de envio para Instagram.
O cliente descartava `error`/`message` do corpo HTTP 400, impedindo distinguir
restrição de formato, contrato ou plataforma.

## Mudança segura

- Nenhuma copy, classificação, etapa, prompt, áudio ou destino foi alterado.
- O cliente Zernio passou a registrar um detalhe técnico curto e sanitizado do
  erro do provedor.
- URLs, credenciais e quebras de linha são removidas antes do log; o detalhe é
  limitado a 160 caracteres.

## Validação

- 124 testes aprovados.
- Typecheck aprovado.
- Build aprovado.
- Teste novo prova preservação do diagnóstico e remoção de URL/credencial.
- Smoke de mídia confirmou upload e acesso ao MP3, sem enviar mensagem a
  usuário.

## Implantação e resultado

- Lambda publicada como versão 7 em `canary`.
- Lambda ativa, atualização bem-sucedida, fila vazia e nenhum erro após deploy.
- Não houve expansão para `live`, pois o áudio real ainda não foi comprovado.
- Não houve rollback: a mudança é apenas observabilidade sanitizada, todos os
  testes passaram e nenhum comportamento de conversa foi alterado.
