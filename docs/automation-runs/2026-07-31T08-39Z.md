# SexyFlow — ciclo automático 2026-07-31 08:39Z

## Escopo

- Janela observada: últimas 3 horas.
- Conta Zernio: `6a1205a62b2567671a24e855`.
- Mídia: `18299164084305199`.
- Destino preservado: `https://comunidade.saraiva.ai/estruturas/prospeccao-automatizada`.

## Evidência

- 45 webhooks Zernio aceitos e 44 comandos concluídos.
- Zero mensagens pendentes na fila principal e zero na DLQ.
- Três sessões da campanha atualizadas na janela.
- Uma jornada chegou a `offering_community`, com `example_opened` e
  `community_cta_sent`.
- Duas perguntas fora do roteiro foram respondidas pelo Bedrock sem alterar
  `awaiting_goal`.
- Uma tentativa de áudio após a correção de IAM gerou o MP3, mas o endpoint de
  mensagens do Zernio respondeu HTTP 400 ao receber diretamente a URL assinada
  do S3.
- Nenhuma duplicação, perda de estado, fato inventado ou erro Lambda foi
  identificado.

## Hipótese

O repasse de uma URL assinada do S3 como `attachmentUrl` não é aceito de forma
confiável pelo transporte Instagram do Zernio. O próprio Zernio oferece
`POST /v1/media/upload-direct` para hospedar temporariamente o arquivo antes do
envio.

## Mudança

- O áudio agora é baixado da URL privada assinada.
- O MP3 é enviado ao `upload-direct` do Zernio.
- A URL temporária retornada é usada como `attachmentUrl`.
- Limite de 25 MB, timeouts e fallback textual foram preservados.
- Nenhum estado, copy, preço, destino ou regra comercial foi alterado.

## Validação

- 123 testes aprovados.
- Typecheck aprovado.
- Build aprovado.
- Smoke real: ElevenLabs gerou 100.772 bytes e o Zernio aceitou o upload com
  HTTP 200, retornando URL em `media.zernio.com`.
- Link rastreado inválido continuou retornando HTTP 403.

## Implantação e resultado

- Lambda publicada como versão 6.
- Modo reduzido de `live` para `canary` antes da implantação.
- Todos os três participantes recentes estão allowlisted e podem validar a
  correção.
- Lambda ativa, atualização bem-sucedida e sem novos erros após a implantação.
- A expansão para `live` fica condicionada a um áudio real enviado com sucesso
  pelo fluxo; não houve rollback porque testes, smoke e saúde do runtime
  permaneceram verdes.
