/**
 * Configuração do PM2 para o respondedor (DESATIVADO).
 *
 * O antigo servidor Contabo foi removido.
 * Responder de comentários/DMs migrado para Chatrace.
 * Disable feito via código + RESPONDER_ENABLED=false.
 */
module.exports = {
  // DESATIVADO - apps antigos removidos
  apps: [],
};
